namespace Strawberry;

public enum CompareFunc
{
    LessEqual,    // x <= y
    Less,         // x < y
    GreaterEqual, // x >= y
    Greater,      // x > y
    Equal,        // x == y
    NotEqual,     // x != y
    Always,       // true
    Never         // false
}
